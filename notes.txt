------------------------
REFACTOR / RESTRUCTURE
------------------------
    ✔ Move StatusBadge into UI
    ✔ No button tag should be in the <td> where no need.
    ✔ Make reusable component to Not result, Loading, Not found, etc messages.
    - When adding comment, shouldn't re-load the table content.
    - Authentication
    - Listening to server/database changes and update acordingly
    - getDurationWithColor: make it scaleable to be able to choos as many intervalls and colors as needed (integrate color style in jsx)
    - Make status badge colors global => easy to scale/change


------------------------
FUNCTIONALITIES / UI / UX
------------------------
    ✔ System message if no match to filter options. At this moment message is displayed only if no match to the serching value.
    ✔ A-Z & Z-A order in collection table
    - What if the status just acidentally changed? How to redo it?
    - Authorisation on login. userId: "9999", // This should come from auth state
    - Lorry spent time plus indicator displayed in the table row:
        / time <= 1h ==> green
        / 1h <= time < 2h ==> yellow
        / time >= 2h ==> red 
    - Title popup when hovering elements/components
    - From-To date picker as filter
    - For status update: in progress, failed SystemMessages
    - When adding comment in the info modal, disable concurens field/buttons
    - In the info modal: option to amend the records
    - If browser refreshes, keep the currently selected filters.
    - But add a filters reset button.
    - Under the badge, display the spent time of that status.
    - setInterval to update the displayed times.


------------------------
STYLES
------------------------
    ✔ Make form element styles global
    - Dark mode in the settings
    - Fix the side bar collaps on event when hamburger icon clicked


------------------------
TO SETTINGS PAGE
------------------------
    - Today's collections checked by default or not
    - Dark mode / Bright mode / Device default
    - Do you want to get notifications, if lorry spent time exceeding 1h, 2h...


------------------------
REPORTS
------------------------
    - Average & sum loading time of the day/week/month/year
    - AM vs PM metrics